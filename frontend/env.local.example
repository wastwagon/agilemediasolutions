# Copy to frontend/.env.local (same folder as package.json) for instant reload during local dev.
# Required when you run `npm run dev` on the host: Next proxies /api to this URL (not http://backend:4000).
# Must match the backend port published in root .env (default BACKEND_EXT_PORT=4005).

BACKEND_INTERNAL_URL=http://localhost:4005
