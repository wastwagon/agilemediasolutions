# Copy to frontend/.env.local (same folder as package.json) for instant reload during local dev.
# API must match the host port mapped for backend in root .env (default BACKEND_EXT_PORT=4005).

BACKEND_INTERNAL_URL=http://localhost:4005
