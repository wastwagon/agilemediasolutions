# Agile Media Solutions — Public Repository

**Modern. Dynamic. Institutional.**

This repository contains the complete codebase for the newly modernized Agile Media Solutions platform. It features a high-performance **Next.js 15+** frontend, a secure **Node.js** CMS backend, and a fully Dockerized infrastructure.

## 🚀 Key Features

*   **📱 Native-App Mobile Navigation**: A fixed glassmorphic bottom navigation bar tailored for high-end mobile user experience.
*   **🛠️ Dynamic CMS**: Fully dynamic "Sectors" and "Brands" pages managed via a PostgreSQL-backed API.
*   **💎 Premium Design System**: A high-contrast, AI-inspired aesthetic with institutional green branding (`#2C504A`).
*   **📦 Docker-Powered**: Single-command deployment using Docker Compose (Frontend, Backend, Postgres, Redis).

## 🏗️ Architecture

- **Frontend**: Next.js (App Router), CSS Modules, Framer Motion.
- **Backend**: Node.js, Express, PG Pool.
- **Database**: PostgreSQL (Structured data), Redis (Caching).
- **Deployment**: Optimized for Coolify/VPS via Docker.

## 🏁 Getting Started

### 1. Requirements
*   **Docker Desktop** (or Docker Engine + Compose)
*   **GitHub Desktop** (for local repo management)

### 2. Launch Local Environment
```bash
docker-compose up -d --build
```
Access the site at: `http://localhost:8085`

### 3. Hot-Reload Development Mode (No Rebuild Needed)
Use the dev override to mount source code into containers with file watching enabled:

```bash
docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d frontend backend
```

In this mode:
- Frontend runs `next dev` with Fast Refresh.
- Backend runs `node --watch index.js` for automatic restart on code changes.
- Most code edits are reflected automatically without `--build`.

To return to production-style containers, run:

```bash
docker compose -f docker-compose.yml down
docker compose up -d --build
```

## 🛠️ CMS Integration
The platform includes built-in API support for:
*   **Sectors CMS**: Manage institutional categories.
*   **Brands CMS**: Portfolio management for media properties.
*   **Contact Management**: Secure database for lead generation.

---

**Generated by Antigravity AI for Agile Media Solutions.**
